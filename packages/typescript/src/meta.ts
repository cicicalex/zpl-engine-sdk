/**
 * Keep in sync with package.json "version" before publish.
 * @module meta
 */
export const SDK_VERSION = '1.0.2';

/** ADR 0002 default for `X-ZPL-Client` from this package. */
export const ZPL_SDK_CLIENT_TYPE = 'sdk-typescript' as const;
